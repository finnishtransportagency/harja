(ns harja-laadunseuranta.core-test
  (:require  [clojure.test :as t :refer [deftest testing is]]
             [harja-laadunseuranta.core :as c]))

(deftest dummy-test
  (testing "Dummy"
    (is (= 1 1))))
